/probe
/desktop/build
node_modules
*.o

# Built binaries (the bare pattern `argus-diff` also matched cmd/argus-diff/).
/argus-diff
/cmd/argus-diff/argus-diff
.gstack/

# Wails desktop build output
/desktop/desktop

# daemon runtime store (written by argusd)
desktop/frontend/public/store/

# argusd daemon build output
/argusd

# Submission archive (regenerate with /zipup)
/argus-submission.zip

# Claude Code: machine-local settings, and agent worktrees (~115MB of live
# checkouts). Project skills under .claude/skills/ ARE tracked.
.claude/settings.local.json
.claude/worktrees/
